import { FC } from "react"
import { Layout } from "Widgets"

const ProfilePage: FC = () => {
    return (
        <Layout>
            profile test text
        </Layout>
    )
}

export default ProfilePage