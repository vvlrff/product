import { Container } from 'Shared'
import { Layout } from 'Widgets'

const LoginPage: React.FC = () => {
  return (
      <Layout>
          <Container>LoginPage Page</Container>
      </Layout>
  )
}

export default LoginPage
